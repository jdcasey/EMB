<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  
  <groupId>org.commonjava.xaven</groupId>
  <artifactId>xaven</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  
  <modules>
    <module>maven-plexus-override</module>
    <module>examples</module>
    <module>nexus</module>
    <module>xaven-resolvers</module>
    <module>xaven-installers</module>
  </modules>
  
  <properties>
    <mavenVersion>2.2.1</mavenVersion>
  </properties>
  
  <dependencyManagement>
    <dependencies>
      <!-- NOTE: These MUST be provided by xaven infrastructure! -->
      <dependency>
        <groupId>com.google.inject</groupId>
        <artifactId>guice</artifactId>
        <version>2.0</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>com.thoughtworks.xstream</groupId>
        <artifactId>xstream</artifactId>
        <version>1.3.1</version>
        <scope>provided</scope>
      </dependency>
      <!-- /END infra requirements -->
      
      <!-- This is the 'kernel' for xaven, and should be a dependency of all xaven extensions. -->
      <dependency>
        <groupId>org.commonjava.xaven.infra</groupId>
        <artifactId>maven-plexus-override</artifactId>
        <version>${project.version}</version>
        <!-- NOTE: This is provided, since we may want to use multiple xaven extensions in a single instance. -->
        <scope>provided</scope>
      </dependency>
      
      <dependency>
        <groupId>org.apache.maven</groupId>
        <artifactId>maven-artifact</artifactId>
        <version>${mavenVersion}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.maven</groupId>
        <artifactId>maven-artifact-manager</artifactId>
        <version>${mavenVersion}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.maven</groupId>
        <artifactId>maven-project</artifactId>
        <version>${mavenVersion}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>junit</groupId>
        <artifactId>junit</artifactId>
        <version>4.5</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
  
  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>2.2</version>
          <configuration>
            <source>1.5</source>
            <target>1.5</target>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>